---
description: Roblox Cat Battle 開發指引，適用於所有程式碼修改與新功能開發
globs: ["**/*.lua", "**/*.json", "**/*.md"]
alwaysApply: true
---

# Roblox Cat Battle — AI 開發指引

> 本規則整合 CLAUDE.md、PRD.md、GAME_DESIGN.md 三份文件的核心資訊，  
> AI 應優先查閱各原始文件取得最新狀態，本規則提供快速導引。

---

## 一、必讀文件順序

處理任何任務前，請依序參照：

1. **`AGENT.md`** — 版本號準則、工作規範、AI 協作協議（**必讀**）
2. **`GAME_DESIGN.md`** — 當前實作狀態總覽（✅/⚠️/❌），先確認功能是否已做
3. **`PRD.md`** — 完整產品需求，包含所有數值、技能、NPC、貨幣定義
4. **`CLAUDE.md`** — 開發環境、Rojo 結構、代碼守則、部署規則

---

## 二、開發環境快速參考

- **語言：** Luau（強型別，盡量使用型別標註）
- **工具：** Rojo（`rojo build --output game.rbxl`）
- **部署：** GitHub Actions 自動 build + Open Cloud API 推送

### 檔案結構對應

| 路徑 | Roblox 位置 |
|---|---|
| `src/server/` | `ServerScriptService` |
| `src/client/` | `StarterPlayer.StarterPlayerScripts` |
| `src/shared/` | `ReplicatedStorage.Shared` |
| `default.project.json` | RemoteEvent / RemoteFunction 定義 |

---

## 三、代碼守則（摘要）

1. 變數與函式 → `camelCase`；服務引用 → `PascalCase`
2. DataStore / 網絡請求 → 必須 `pcall`
3. 邏輯放 `ModuleScript`，透過 `require` 呼叫
4. **禁止**直接修改 `.rbxl` 二進制檔
5. 新增 RemoteEvent → 修改 `default.project.json` 的 `ReplicatedStorage.RemoteEvents`

---

## 四、系統實作狀態（快速查閱，詳見 GAME_DESIGN.md）

| 系統 | 後端 | 前端 UI |
|---|---|---|
| 玩家資料 / DataStore | ✅ | — |
| 貓咪選擇 | ✅ | ✅ |
| 貓咪外觀（全身替換） | ✅ | — |
| 等級 / 經驗值 | ✅ | ✅ |
| 技能系統 | ✅ | ✅ |
| 基礎攻擊 / 點擊 NPC | ✅ | ✅ |
| 武器 / 裝備系統 | ✅ | ✅ |
| 商城（Robux 買貓） | ✅ | ✅ |
| 商城（金幣買裝備） | ✅ | ✅ |
| 寵物合成系統 | ✅ | ✅ |
| NPC 生成 / AI | ✅ | — |
| PvP 系統 | ✅ | ✅ |
| 地圖 / 場景 | ✅ | — |
| HP 顯示 / 自動回復 | ✅ | — |
| 3D 商店攤位（ProximityPrompt） | ✅ | ✅ |

---

## 五、重要數值速查（詳見 PRD.md）

### 升級公式
```
xpRequired(level) = floor(80 × level^1.85 + 20 × level)
```

### NPC XP 倍率
- 玩偶 ×1.0、野貓 ×1.25、野人 ×1.5

### 碎片合成機率
- 玩偶：8%、野貓：15%、野人：22%；需收集 **10 個**相同碎片

---

## 六、RemoteEvent 清單（詳見 GAME_DESIGN.md 與 default.project.json）

已定義的 RemoteEvent（全部在 `ReplicatedStorage.RemoteEvents`）：

`RequestLoadData` / `LoadDataResponse` / `AddXP` / `LevelUp` / `UpdateCatAppearance` /  
`SelectCat` / `SpawnNPC` / `NPCDied` / `PurchaseCat` / `PurchaseResult` /  
`EquipItem` / `UnequipItem` / `EquipmentChanged` / `BuyEquipment` /  
`UseSkill` / `SkillResult` / `SkillUnlocked` / `CombatHit` / `PlayerDied` / `UpdateUI` /  
`RequestPvP` / `PvPInvite` / `PvPAccepted` / `PvPResult` /  
`SynthesizeCat` / `SynthesisResult` / `UpdateFragments`

RemoteFunction：`GetPlayerData` / `GetShopCatalog`

---

## 七、每次修改後的強制步驟

1. **遞增 `GameConfig.VERSION`**（`src/shared/GameConfig.lua`）
2. **更新 `GAME_DESIGN.md`** 的「系統實作狀態總覽」與「變更日誌」
3. 標記功能狀態：✅ 完成 / ⚠️ 部分 / ❌ 未做
4. `git commit -m "v0.1.X feat/fix: 描述"`（訊息開頭必須有版本號）
5. `git push`
6. PR 標題格式：`v0.1.X 功能描述`

### 版本號定義位置
- `src/shared/GameConfig.lua`：`GameConfig.VERSION = "v0.1.X"`
- UI 自動從此處讀取顯示於畫面左下角（`🐾 v0.1.X`）

---

## 八、攻擊視覺特效（VFX）系統

位置：`src/client/CombatClient.lua`

### 架構

```
spawnAttackVFX(skillId, hitPos)
  ├── 武器底層特效  WEAPON_VFX[currentWeapon](hitPos)
  └── 技能疊加特效  SKILL_VFX[skillId](hitPos)
```

- `CombatClient.currentWeapon`：由 `GameClient` 在 `EquipmentChanged` 與 `LoadDataResponse` 時同步
- 攻擊指令發出後立即播放特效，不等伺服器回應（手感即時）

### 武器特效對應

| 武器 ID | 特效風格 |
|---|---|
| weaponClaws | 銀灰三條爪痕 + 小衝擊球 |
| weaponSword | 藍白劍氣爪痕 + 橫向環 |
| weaponShield | 金色雙環衝擊波 + 大球 |
| weaponStaff | 紫色星爆 + 上升魔法粒子 |
| 無武器 | 白色三條爪痕 + 小球 |

### VFX 基礎函數

| 函數 | 用途 |
|---|---|
| `vfxBurst(pos, color, startSize, endSize, duration)` | 球形擴散後淡出 |
| `vfxRing(pos, color, startR, endR, duration)` | 圓環向外展開 |
| `vfxClawSlash(pos, color, count, length)` | 平行爪痕條紋 |
| `vfxRise(pos, color, count)` | 粒子向上噴射 |
| `vfxLightning(pos, color, segments)` | 隨機鋸齒閃電段 |

## 九、NPC AI 設計

NPC 採三狀態機（`NPCManager.lua` 的 `npcState` 表）：

| 狀態 | 觸發條件 | 行為 |
|---|---|---|
| `idle` | 初始 / 返回原位後 | 漫步（home ±18 格），速度 × 0.5 |
| `chase` | 感知範圍（28 格）內有玩家且不在安全區 | 全速追逐 |
| `return` | 玩家進安全區 / 玩家跑超出 50 格 / 離 home > 65 格 | 速度 × 1.5 快速返回 home |

安全區（`SAFE_ZONES`，NPCManager.lua 頂部定義，需與 WorldSetup 的視覺保持一致）：
- 出生點圓形：center=(0,0,0)，radius=55
- 商城矩形：X=−42~42，Z=10~72（完整覆蓋三棟商店建築）

WorldSetup 視覺：Mint 青綠色 SmoothPlastic 地板（透明度 0.72）+ BillboardGui 文字標示

玩偶（doll）不移動，只在攻擊距離（5 格）內且玩家不在安全區時攻擊。

## 九、3D 商店攤位架構

出生點南方（Z = +40）三棟攤位，由 `WorldSetup.server.lua` 建立：

| 建築名稱 | Attribute `ShopAction` | 開啟分頁 |
|---|---|---|
| ShopBuilding（黃金色） | `OpenShop` | 商城貓咪 tab |
| EquipBuilding（藍色） | `OpenEquip` | 商城裝備 tab |
| SynthBuilding（紫色） | `OpenSynth` | 商城合成 tab |

- `ProximityPrompt` 掛在各建築的 Body 上，Name = `ShopPrompt`，MaxActivationDistance = 12
- `GameClient.client.lua` 的 `bindShopPrompts()` 掃描 workspace，連接到 `UIManager.openShopPanel(startTab)`
- `UIManager.openShopPanel(startTab)` 支援 `"cats"` / `"equip"` / `"synth"` 三種參數

## 九、卡片視覺設計

商城面板卡片（`buildCard`）支援以下視覺元素：
- **左側色條**（5px）：以 `ICON_COLORS[iconKey]` 著色
- **左側色塊**（42×74%）：半透明識別色塊，顯示名稱首字大寫
- **合成進度條**：傳入 `fragData = {current, max}` 時，在卡片底部顯示碎片進度條
- Tab 按鈕支援高亮（當前 tab 深藍色，其餘深色）
